# Polished Portfolio `src` (React + Tailwind)

## What this contains
- Minimal, elegant React components
- Tailwind-ready `index.css`
- Components: Header, Hero, About, Skills, Projects, Contact, Footer

## Quick start (Vite)
1. Create a new Vite app:
   npm create vite@latest my-portfolio -- --template react
2. Install dependencies and Tailwind:
   npm install
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
3. Update `tailwind.config.js` content paths:
   ./index.html, ./src/**/*.{js,jsx,ts,tsx}
4. Replace `/src` of the new project with this folder.
5. Start dev server: `npm run dev`

## Notes
- Update contact links (email, GitHub, LinkedIn).
- Replace any placeholder text if you want personal phrasing.
- Ask me to integrate fonts (Inter/Google), dark mode toggle, or animations.
